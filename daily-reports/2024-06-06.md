

```js
class ListNode {
	constructor(val = 0, next = null) {
		this.val = val;
		this.next = next;
	}
}


class AddTwoNumbers {
	function addTwo(a, b){
		carry = 0;
		reverse a, b
		for idx in max(len(a), len(b)){
			va = a[idx] if a[idx] exists else 0
			vb = ...
			sum = va + vb + carry
			remainder = sum%10
			carry = remainder//10
			results.append(remainder)
		}
		return reverse 
	}

}


module.exports = { Problem: AddTwoNumbers };

```