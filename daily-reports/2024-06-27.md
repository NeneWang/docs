

```
class Solution {	// Your code here

	solve(head, m, n){
		
		// Reverse like this 3 -> 4 -> 10 into  3 <- 4 <- 10
		traverse.. until position
		dist = m - n
		
		curr= head.next
		prev = head
		while curr and dist > 0:
			next = curr.next
			curr.next = prev
			prev = curr
			curr = next
			dist -= 1
			

	}
tajts ti 
}


module.exports = { Problem: Solution };
```


